
  export enum VoteType {
    Against,
    For,
    Abstain,
  }

  export enum ProposalState {
    Pending,
    Active,
    Canceled,
    Defeated,
    Succeeded,
    Queued,
    Expired,
    Executed,
  }